<Sidebar>
    <div>
        <LinkTo @route="locations.new">
            <Button type="button">
                {{t "location.new"}}
            </Button>
        </LinkTo>
    </div>
    {{#each @model as |location|}}
        <Sidebar::Location @location={{location}} @didLocationChange={{this.refresh}}/>
    {{/each}}
</Sidebar>
<div class="w-75 fr">
{{outlet}}
</div>
