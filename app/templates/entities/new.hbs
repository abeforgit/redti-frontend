<div>
    <label for="name">Name</label>
    <input type="text" name="name" value={{this.name}}
           {{on "input" (pick "target.value" (set this.name))}}
    />
    <button type="button" {{on "click" this.saveEntity}}>Save</button>
</div>
